/**
 * Option shell: sidebar branding row + main content page title bars share this height
 * (h-14 ≈ 32px logo + vertical padding) so they align across the layout.
 */
export const OPTIONS_SHELL_HEADER_ROW =
  "flex h-14 shrink-0 items-center border-b border-border" as const;
